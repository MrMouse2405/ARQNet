--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--#selene: allow(shadowing)
-- File generated by Blink v0.18.6 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
export type HitData = { hitX: number, hitY: number, hitZ: number, limbId: number, normX: number, normY: number, normZ: number, materialId: number }
export type InventoryItem = { uuid: string, name: string, qty: number, enchanted: boolean, durability: number }
export type Input = { dt: number, x: number, y: number, jump: number, crouch: number }
local function WriteEVENT_fps_hitreg(Value: { weaponId: number, timestamp: number, originX: number, originY: number, originZ: number, hitData: { { hitX: number, hitY: number, hitZ: number, limbId: number, normX: number, normY: number, normZ: number, materialId: number } } }): ()
	-- Allocate BLOCK: 24 bytes
	local BLOCK_START = Allocate(24)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.weaponId)
	buffer.writef64(SendBuffer, BLOCK_START + 2, Value.timestamp)
	buffer.writef32(SendBuffer, BLOCK_START + 10, Value.originX)
	buffer.writef32(SendBuffer, BLOCK_START + 14, Value.originY)
	buffer.writef32(SendBuffer, BLOCK_START + 18, Value.originZ)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 22, #Value.hitData)
		-- Allocate BLOCK: 26 bytes
	local ARRAY_START_1 = Allocate(26 * #Value.hitData)
	for Index = 1, #Value.hitData do
		local Item_1 = Value.hitData[Index]
		-- Allocate 4
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_0, Item_1.hitX)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Item_1.hitY)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Item_1.hitZ)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.limbId)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Item_1.normX)
		-- Allocate 4
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_5, Item_1.normY)
		-- Allocate 4
		local OPERATION_OFFSET_6 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_6, Item_1.normZ)
		-- Allocate 1
		local OPERATION_OFFSET_7 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_7, Item_1.materialId)
	end
	-- END ARRAY
end

local function WriteEVENT_input_stream(Value: { { dt: number, x: number, y: number, jump: number, crouch: number } }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 1, #Value)
		-- Allocate BLOCK: 18 bytes
	local ARRAY_START_1 = Allocate(18 * #Value)
	for Index = 1, #Value do
		local Item_1 = Value[Index]
		-- Allocate 8
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 8
		buffer.writef64(SendBuffer, OPERATION_OFFSET_0, Item_1.dt)
		-- Allocate 4
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Item_1.x)
		-- Allocate 4
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Item_1.y)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.jump)
		-- Allocate 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.crouch)
	end
	-- END ARRAY
end

local function WriteEVENT_save_file_load(Value: { userId: number, description: string, inventory: { { uuid: string, name: string, qty: number, enchanted: boolean, durability: number } }, stats: {[string]: number}, questsCompleted: { boolean } }): ()
	-- Allocate BLOCK: 17 bytes
	local BLOCK_START = Allocate(17)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	buffer.writef64(SendBuffer, BLOCK_START + 1, Value.userId)
	local Length = #Value.description
	buffer.writeu16(SendBuffer, BLOCK_START + 9, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.description, Length)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 11, #Value.inventory)
		-- Allocate BLOCK: 10 bytes
	local ARRAY_START_1 = Allocate(10 * #Value.inventory)
	for Index = 1, #Value.inventory do
		local Item_1 = Value.inventory[Index]
		local Length = #Item_1.uuid
		-- Allocate 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.uuid, Length)
		local Length = #Item_1.name
		-- Allocate 2
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 2
		buffer.writeu16(SendBuffer, OPERATION_OFFSET_1, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Item_1.name, Length)
		-- Allocate 1
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_2, Item_1.qty)
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.enchanted and 1 or 0)
		-- Allocate 4
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 4
		buffer.writef32(SendBuffer, OPERATION_OFFSET_4, Item_1.durability)
	end
	-- END ARRAY
	local Elements_1 = 0
	for Key_1, Element_1 in Value.stats do
		Elements_1 += 1
		local Length = #Key_1
		local OFFSET_0 = Allocate(2)
		buffer.writeu16(SendBuffer, OFFSET_0, Length)
		Allocate(Length)
		buffer.writestring(SendBuffer, SendOffset, Key_1, Length)
		local OFFSET_1 = Allocate(1)
		buffer.writeu8(SendBuffer, OFFSET_1, Element_1)
	end
	buffer.writeu16(SendBuffer, BLOCK_START + 13, Elements_1)
	-- START ARRAY
	buffer.writeu16(SendBuffer, BLOCK_START + 15, #Value.questsCompleted)
		-- Allocate BLOCK: 1 bytes
	local ARRAY_START_1 = Allocate(1 * #Value.questsCompleted)
	for Index = 1, #Value.questsCompleted do
		local Item_1 = Value.questsCompleted[Index]
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1 and 1 or 0)
	end
	-- END ARRAY
end

local function WriteEVENT_voxel_chunk(Value: buffer): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	local Length = buffer.len(Value)
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.copy(SendBuffer, SendOffset, Value, 0, Length)
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	fps_hitreg = {
		Fire = NOOP
	},
	input_stream = {
		Fire = NOOP
	},
	save_file_load = {
		Fire = NOOP
	},
	voxel_chunk = {
		Fire = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	fps_hitreg = {
		Fire = function(Value: { weaponId: number, timestamp: number, originX: number, originY: number, originZ: number, hitData: { { hitX: number, hitY: number, hitZ: number, limbId: number, normX: number, normY: number, normZ: number, materialId: number } } }): ()
			WriteEVENT_fps_hitreg(Value)
		end
	},
	input_stream = {
		Fire = function(Value: { { dt: number, x: number, y: number, jump: number, crouch: number } }): ()
			WriteEVENT_input_stream(Value)
		end
	},
	save_file_load = {
		Fire = function(Value: { userId: number, description: string, inventory: { { uuid: string, name: string, qty: number, enchanted: boolean, durability: number } }, stats: {[string]: number}, questsCompleted: { boolean } }): ()
			WriteEVENT_save_file_load(Value)
		end
	},
	voxel_chunk = {
		Fire = function(Value: buffer): ()
			WriteEVENT_voxel_chunk(Value)
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL